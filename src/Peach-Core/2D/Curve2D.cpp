#include "../../include/Peach-Core/2D/Curve2D.h"

namespace PeachCore {

   /*float sin(float stretch, float horizontalShift, float x, float power)
    {
        return stretch * (float)Math.Pow(Math.Sin(x + horizontalShift), power);
    }

    float cos(float stretch, float horizontalShift, float x, float power)
    {
        return stretch * (float)Math.Pow(Math.Cos(x + horizontalShift), power);
    }

    float log(float stretch, float horizontalShift, float x, float power)
    {
        return stretch * (float)Math.Pow(Math.Log(x + horizontalShift), power);
    }

    float root(float stretch, float horizontalShift, float x, float power)
    {
        return stretch * (float)Math.Pow((x + horizontalShift), 1 / power);
    }*/
}